[package]
name = "truthmill"
version = "0.1.0"
edition = "2024"

[dependencies]
num-rational = { version = "0.4", default-features = false, features = ["serde", "std"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
